require("toggleterm").setup({
	-- Optional: if you want the terminal to be a specific size
	size = 10,
	-- Optional: if you want to start in insert mode
	open_mapping = [[<Leader>tt]],
	autoinsert = true,
})
